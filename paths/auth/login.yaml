post:
  tags:
    - user
    - auth
  summary: Logs the user in
  operationId: login
  responses:
  requestBody:
    content:
      application/x-www-form-urlencoded:
        schema:
          $ref: './../../requests/user/user-login-dto.yaml'